#include "./common.hlsl"

Texture2D<float4> t2 : register(t2);
Texture2D<float4> t1 : register(t1);
Texture2D<float4> t0 : register(t0);
SamplerState s13_s : register(s13);
SamplerState s12_s : register(s12);
cbuffer cb5 : register(b5){
  float4 cb5[15];
}

#define cmp -

void main(
  float4 v0 : SV_Position0,
  float4 v1 : TEXCOORD0,
  out float4 o0 : SV_Target0)
{
  float4 r0,r1,r2,r3,r4;
  uint4 bitmask, uiDest;
  float4 fDest;

  r0.x = cb5[8].x + -cb5[8].y;
  r0.y = v1.x * 2 + -1;
  r1.x = cb5[2].y * r0.y;
  r1.y = v1.y * -2 + 1;
  r0.yz = cb5[2].zz * r1.xy;
  r0.w = dot(r0.yz, r0.yz);
  r0.w = sqrt(r0.w);
  r1.zw = r0.yz * r0.ww + -r0.yz;
  r2.xy = r0.yz * r0.ww;
  r0.yz = r1.zw * float2(-0.5,-0.5) + r0.yz;
  r3.x = cb5[2].y * cb5[1].x;
  r3.y = cb5[1].y;
  r1.zw = cb5[2].zz * r3.xy;
  r2.zw = -r3.xy + r1.xy;
  r1.xy = r1.xy * cb5[2].zz + -r1.zw;
  r0.w = dot(r2.zw, r2.zw);
  r0.w = sqrt(r0.w);
  r0.w = 1 + -r0.w;
  r0.w = max(0, r0.w);
  r0.w = r0.w * r0.w;
  r0.w = r0.w * r0.w;
  r2.zw = r0.xx * r1.zw + r0.yz;
  r0.x = dot(r2.zw, r2.zw);
  r0.x = sqrt(r0.x);
  r0.x = log2(r0.x);
  r0.x = cb5[8].z * r0.x;
  r0.x = exp2(r0.x);
  r0.x = 0.00999999978 + -r0.x;
  r0.x = max(0, r0.x);
  r0.x = cb5[7].x * r0.x;
  r0.x = cb5[8].w * r0.x;
  r0.x = 4 * r0.x;
  r2.z = cb5[6].x + -cb5[6].y;
  r2.zw = r2.zz * r1.zw + r2.xy;
  r2.z = dot(r2.zw, r2.zw);
  r2.z = sqrt(r2.z);
  r2.z = log2(r2.z);
  r2.z = cb5[6].z * r2.z;
  r2.z = exp2(r2.z);
  r2.z = r2.z * 640 + 1;
  r2.w = dot(r1.xy, r1.xy);
  r3.x = sqrt(r2.w);
  r2.w = r2.w * 640 + 4;
  r2.w = rcp(r2.w);
  r3.x = log2(r3.x);
  r3.x = 0.100000001 * r3.x;
  r3.x = exp2(r3.x);
  r2.z = r3.x / r2.z;
  r2.z = cb5[5].x * r2.z;
  r3.y = min(1, r3.x);
  r2.z = r3.y * r2.z;
  r0.x = r2.z * cb5[6].w + r0.x;
  r2.z = cb5[10].x + -cb5[10].y;
  r3.zw = r2.zz * r1.zw + r0.yz;
  r2.z = dot(r3.zw, r3.zw);
  r2.z = sqrt(r2.z);
  r2.z = log2(r2.z);
  r2.z = cb5[10].z * r2.z;
  r2.z = exp2(r2.z);
  r2.z = 0.00999999978 + -r2.z;
  r2.z = max(0, r2.z);
  r2.z = cb5[9].x * r2.z;
  r2.z = cb5[10].w * r2.z;
  r0.x = r2.z * 4 + r0.x;
  r2.z = cb5[12].x + -cb5[12].y;
  r3.zw = r2.zz * r1.zw + r0.yz;
  r2.z = dot(r3.zw, r3.zw);
  r2.z = sqrt(r2.z);
  r2.z = log2(r2.z);
  r2.z = cb5[12].z * r2.z;
  r2.z = exp2(r2.z);
  r2.z = 0.00999999978 + -r2.z;
  r2.z = max(0, r2.z);
  r2.z = cb5[11].x * r2.z;
  r2.z = cb5[12].w * r2.z;
  r0.x = r2.z * 4 + r0.x;
  r2.z = cb5[14].x + -cb5[14].y;
  r3.zw = r2.zz * r1.zw + r2.xy;
  r2.z = dot(r3.zw, r3.zw);
  r2.z = sqrt(r2.z);
  r2.z = log2(r2.z);
  r2.z = cb5[14].z * r2.z;
  r2.z = exp2(r2.z);
  r2.z = 0.00999999978 + -r2.z;
  r2.z = max(0, r2.z);
  r2.z = cb5[13].x * r2.z;
  r2.z = r2.z * r3.y;
  r2.z = cb5[14].w * r2.z;
  r0.x = r2.z * 4 + r0.x;
  r2.z = dot(r2.xy, r2.xy);
  r2.z = sqrt(r2.z);
  r2.zw = float2(0.0500000007,3) * r2.zw;
  r0.x = r0.x * 1.29999995 + -r2.z;
  r3.z = min(abs(r1.x), abs(r1.y));
  r3.w = max(abs(r1.x), abs(r1.y));
  r3.z = r3.z / r3.w;
  r3.w = r3.z * r3.z;
  r4.x = r3.w * -0.0464964733 + 0.159314215;
  r4.x = r4.x * r3.w + -0.327622771;
  r3.w = r4.x * r3.w;
  r3.z = r3.w * r3.z + r3.z;
  r3.w = 1.57079637 + -r3.z;
  r4.x = cmp(abs(r1.x) < abs(r1.y));
  r1.xy = cmp(r1.xy < float2(0,0));
  r3.z = r4.x ? r3.w : r3.z;
  r3.w = 3.14159274 + -r3.z;
  r1.x = r1.x ? r3.w : r3.z;
  r1.y = r1.y ? -1 : 1;
  r1.x = r1.x * r1.y;
  r1.y = 0.075000003 * r1.x;
  r3.zw = r1.zw * float2(0.5,0.5) + float2(0.5,0.5);
  r3.z = r3.z + r3.w;
  r3.w = cb5[4].y * r3.z;
  r1.x = r3.z * cb5[4].y + r1.x;
  r1.x = cb5[4].x * r1.x;
  r1.x = sin(r1.x);
  r4.x = r3.w * 0.100000001 + r1.y;
  r4.yw = float2(0,1);
  r1.y = t0.Sample(s13_s, r4.xy).x;
  r1.y = cb5[4].x * r1.y;
  r1.y = 0.649999976 * r1.y;
  r1.y = sin(r1.y);
  r1.x = r1.y + r1.x;
  r1.y = 0.5 * r3.x;
  r1.x = r1.x * cb5[4].w + r1.y;
  r1.x = 0.975000024 + r1.x;
  r1.x = r2.w * r1.x;
  r1.x = max(r2.w, r1.x);
  r1.x = cb5[4].z * r1.x;
  r0.x = r1.x * cb5[3].x + r0.x;
  r0.x = max(0, r0.x);
  r4.x = cb5[2].x * r0.x;
  r0.x = cb5[6].x + cb5[6].y;
  r3.zw = r0.xx * r1.zw + r2.xy;
  r0.x = dot(r3.zw, r3.zw);
  r0.x = sqrt(r0.x);
  r0.x = log2(r0.x);
  r0.x = cb5[6].z * r0.x;
  r0.x = exp2(r0.x);
  r0.x = r0.x * 640 + 1;
  r0.x = r3.x / r0.x;
  r0.x = cb5[5].z * r0.x;
  r0.x = r0.x * r3.y;
  r1.y = cb5[8].x + cb5[8].y;
  r3.zw = r1.yy * r1.zw + r0.yz;
  r1.y = dot(r3.zw, r3.zw);
  r1.y = sqrt(r1.y);
  r1.y = log2(r1.y);
  r1.y = cb5[8].z * r1.y;
  r1.y = exp2(r1.y);
  r1.y = 0.00999999978 + -r1.y;
  r1.y = max(0, r1.y);
  r1.y = cb5[7].z * r1.y;
  r1.y = cb5[8].w * r1.y;
  r1.y = 4 * r1.y;
  r0.x = r0.x * cb5[6].w + r1.y;
  r1.y = cb5[10].x + cb5[10].y;
  r3.zw = r1.yy * r1.zw + r0.yz;
  r1.y = dot(r3.zw, r3.zw);
  r1.y = sqrt(r1.y);
  r1.y = log2(r1.y);
  r1.y = cb5[10].z * r1.y;
  r1.y = exp2(r1.y);
  r1.y = 0.00999999978 + -r1.y;
  r1.y = max(0, r1.y);
  r1.y = cb5[9].z * r1.y;
  r1.y = cb5[10].w * r1.y;
  r0.x = r1.y * 4 + r0.x;
  r1.y = cb5[12].x + cb5[12].y;
  r3.zw = r1.yy * r1.zw + r0.yz;
  r1.y = dot(r3.zw, r3.zw);
  r1.y = sqrt(r1.y);
  r1.y = log2(r1.y);
  r1.y = cb5[12].z * r1.y;
  r1.y = exp2(r1.y);
  r1.y = 0.00999999978 + -r1.y;
  r1.y = max(0, r1.y);
  r1.y = cb5[11].z * r1.y;
  r1.y = cb5[12].w * r1.y;
  r0.x = r1.y * 4 + r0.x;
  r1.y = cb5[14].x + cb5[14].y;
  r3.zw = r1.yy * r1.zw + r2.xy;
  r1.y = dot(r3.zw, r3.zw);
  r1.y = sqrt(r1.y);
  r1.y = log2(r1.y);
  r1.y = cb5[14].z * r1.y;
  r1.y = exp2(r1.y);
  r1.y = 0.00999999978 + -r1.y;
  r1.y = max(0, r1.y);
  r1.y = cb5[13].z * r1.y;
  r1.y = r1.y * r3.y;
  r1.y = cb5[14].w * r1.y;
  r0.x = r1.y * 4 + r0.x;
  r0.x = r0.x * 1.29999995 + -r2.z;
  r0.x = r1.x * cb5[3].z + r0.x;
  r0.x = max(0, r0.x);
  r4.z = cb5[2].x * r0.x;
  r3.zw = cb5[6].xx * r1.zw + r2.xy;
  r2.xy = cb5[14].xx * r1.zw + r2.xy;
  r0.x = dot(r2.xy, r2.xy);
  r0.x = sqrt(r0.x);
  r0.x = log2(r0.x);
  r0.x = cb5[14].z * r0.x;
  r0.x = exp2(r0.x);
  r0.x = 0.00999999978 + -r0.x;
  r0.x = max(0, r0.x);
  r0.x = cb5[13].y * r0.x;
  r0.x = r0.x * r3.y;
  r0.x = cb5[14].w * r0.x;
  r1.y = dot(r3.zw, r3.zw);
  r1.y = sqrt(r1.y);
  r1.y = log2(r1.y);
  r1.y = cb5[6].z * r1.y;
  r1.y = exp2(r1.y);
  r1.y = r1.y * 640 + 1;
  r1.y = r3.x / r1.y;
  r1.y = cb5[5].y * r1.y;
  r1.y = r1.y * r3.y;
  r2.xy = cb5[8].xx * r1.zw + r0.yz;
  r2.x = dot(r2.xy, r2.xy);
  r2.x = sqrt(r2.x);
  r2.x = log2(r2.x);
  r2.x = cb5[8].z * r2.x;
  r2.x = exp2(r2.x);
  r2.x = 0.00999999978 + -r2.x;
  r2.x = max(0, r2.x);
  r2.x = cb5[7].y * r2.x;
  r2.x = cb5[8].w * r2.x;
  r2.x = 4 * r2.x;
  r1.y = r1.y * cb5[6].w + r2.x;
  r2.xy = cb5[10].xx * r1.zw + r0.yz;
  r0.yz = cb5[12].xx * r1.zw + r0.yz;
  r0.y = dot(r0.yz, r0.yz);
  r0.y = sqrt(r0.y);
  r0.y = log2(r0.y);
  r0.y = cb5[12].z * r0.y;
  r0.y = exp2(r0.y);
  r0.y = 0.00999999978 + -r0.y;
  r0.y = max(0, r0.y);
  r0.y = cb5[11].y * r0.y;
  r0.y = cb5[12].w * r0.y;
  r0.z = dot(r2.xy, r2.xy);
  r0.z = sqrt(r0.z);
  r0.z = log2(r0.z);
  r0.z = cb5[10].z * r0.z;
  r0.z = exp2(r0.z);
  r0.z = 0.00999999978 + -r0.z;
  r0.z = max(0, r0.z);
  r0.z = cb5[9].y * r0.z;
  r0.z = cb5[10].w * r0.z;
  r0.z = r0.z * 4 + r1.y;
  r0.y = r0.y * 4 + r0.z;
  r0.x = r0.x * 4 + r0.y;
  r0.x = r0.x * 1.29999995 + -r2.z;
  r0.x = r1.x * cb5[3].y + r0.x;
  r0.x = max(0, r0.x);
  r4.y = cb5[2].x * r0.x;
  r1.xyzw = cb5[1].wwww * r4.xyzw;
  r0.xyz = t2.Sample(s12_s, v1.xy).xyz;
  r0.xyz = cb5[0].yyy * r0.xyz;
  r2.xyz = t1.Sample(s12_s, v1.xy).xyz;
  r2.xyz = r2.xyz * cb5[0].xxx + r0.xyz;
  r2.w = 1;
  o0.xyzw = r2.xyzw * r0.wwww * injectedData.fxLensDirt + r1.xyzw * injectedData.fxLensFlare;
  return;
}